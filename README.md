# macOS Supplement

## macOS Requirements

- An Apple Silicon CPU or 64-bit Intel CPU
- macOS Sonoma (14) (or higher) installed on officially supported hardware

## Installation Instructions

Use the following command to clone and run the script on a fresh macOS installation

```
git clone https://github.com/macmannes/macos-supplement.git && cd macos-supplement && ./run.sh
```
